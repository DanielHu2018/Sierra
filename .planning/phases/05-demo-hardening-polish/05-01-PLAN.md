---
phase: 05-demo-hardening-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/MapCanvas/MapCanvas.test.tsx
  - src/components/Sidebar/Sidebar.test.tsx
  - src/utils/contrast.test.ts
autonomous: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-06

must_haves:
  truths:
    - "Running the test suite produces red (failing) tests for each Wave 0 scaffold — confirms tests are actually exercising new behavior"
    - "MapCanvas.test.tsx has failing tests for: out-of-bounds click does not call setSourcePin, out-of-bounds click sets oobPopup state, footnote text is present in render"
    - "Sidebar.test.tsx has failing tests for: simulationStatus='error' renders error message and Retry button"
    - "contrast.test.ts has static assertions that would fail if any route or heatmap color dropped below WCAG AA thresholds"
  artifacts:
    - path: "src/components/MapCanvas/MapCanvas.test.tsx"
      provides: "DATA-03 bounds check tests + DATA-05 footnote presence test"
    - path: "src/components/Sidebar/Sidebar.test.tsx"
      provides: "DATA-04 error state branch tests"
    - path: "src/utils/contrast.test.ts"
      provides: "DATA-06 WCAG AA static color assertions"
  key_links:
    - from: "src/components/MapCanvas/MapCanvas.test.tsx"
      to: "src/components/MapCanvas/MapCanvas.tsx"
      via: "component render + handleClick mock"
      pattern: "oobPopup|setSourcePin|setDestinationPin"
    - from: "src/components/Sidebar/Sidebar.test.tsx"
      to: "src/components/Sidebar/Sidebar.tsx"
      via: "simulationStatus prop mock"
      pattern: "simulationStatus.*error|Retry"
---

<objective>
Create Wave 0 test scaffolds for all Phase 5 behavioral requirements. These tests must be written BEFORE implementation so they fail red now and go green when the corresponding implementation tasks complete.

Purpose: Nyquist compliance — every implementation task has an automated verify command pointing to an existing test file.
Output: Three test files covering DATA-03 (bounds check), DATA-04 (error state), DATA-05 (footnote text), DATA-06 (WCAG AA contrast).
</objective>

<execution_context>
@C:/Users/hudan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hudan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-demo-hardening-polish/05-CONTEXT.md
@.planning/phases/05-demo-hardening-polish/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MapCanvas test scaffolds (DATA-03 bounds check + DATA-05 footnote)</name>
  <files>src/components/MapCanvas/MapCanvas.test.tsx</files>
  <behavior>
    - Test A: Calling handleClick with lng=-120, lat=30 (outside ERCOT bbox [-106.6,25.8,-93.5,36.5]) does NOT call setSourcePin or setDestinationPin
    - Test B: Calling handleClick with out-of-bounds coords sets an oobPopup state (confirmed by querying for text "Outside ERCOT coverage area" in the rendered output OR by checking that a Popup with that text is present)
    - Test C: Calling handleClick with lng=-100, lat=30 (inside bbox) DOES call setSourcePin (when no source pin set yet)
    - Test D: The MapCanvas render output contains the text "Illustrative mock data" somewhere in the DOM
  </behavior>
  <action>
    Extend (or create if it does not exist) `src/components/MapCanvas/MapCanvas.test.tsx`.

    Use Vitest + React Testing Library (already installed). Mock the Zustand store (useAppStore) so setSourcePin and setDestinationPin are vi.fn() spies. Mock react-map-gl so the Map component renders its children and the MapLayerMouseEvent can be simulated.

    Write the four failing tests described in the behavior block. They MUST fail before implementation (Task 1 in plan 05-02) and pass after.

    Use `describe('MapCanvas — bounds check', ...)` and `describe('MapCanvas — footnote', ...)` as test groups.

    After writing, run `npx vitest run src/components/MapCanvas/MapCanvas.test.tsx` and confirm the new tests appear as FAIL (red) in the output. If the file already has passing tests for in-bounds behavior, those can remain green — only the NEW tests for this phase should be red.
  </action>
  <verify>
    <automated>npx vitest run src/components/MapCanvas/MapCanvas.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Four new test cases exist and the two bounds-check tests + footnote test fail with "AssertionError" (not import/syntax errors). The suite runs without crashing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sidebar error state test scaffold (DATA-04)</name>
  <files>src/components/Sidebar/Sidebar.test.tsx</files>
  <behavior>
    - Test A: When Sidebar is rendered with simulationStatus='error' (via mocked Zustand store), the text "Route generation failed. Please retry." appears in the DOM
    - Test B: When Sidebar is rendered with simulationStatus='error', a button with text "Retry" is present
    - Test C: When simulationStatus='idle', neither the error message nor Retry button appear
  </behavior>
  <action>
    Create `src/components/Sidebar/Sidebar.test.tsx` (new file).

    Use Vitest + React Testing Library. Mock useAppStore to return simulationStatus='error' for the failing cases and simulationStatus='idle' for the passing control case. Render Sidebar and assert text presence.

    These tests MUST fail before plan 05-03 implements the error branch. Confirm by running the test command below — expect FAIL on tests A and B (the 'error' branch does not exist yet in Sidebar.tsx).
  </action>
  <verify>
    <automated>npx vitest run src/components/Sidebar/Sidebar.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Three test cases exist. Tests A and B fail red (component doesn't render error state yet). Test C may pass or fail depending on existing Sidebar implementation — either is acceptable at this stage. Suite runs without import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WCAG AA contrast static assertions (DATA-06)</name>
  <files>src/utils/contrast.test.ts</files>
  <behavior>
    - For each color, the computed WCAG contrast ratio against #131313 must be >= 4.5 (normal text AA) or >= 3.0 (UI component AA)
    - Colors to assert: #A7C8FF (route A, UI component >= 3.0), #FFBC7C (route B, UI component >= 3.0), #E8B3FF (route C, UI component >= 3.0), #3291FF (heatmap low, UI component >= 3.0), #FF4444 (heatmap high, UI component >= 3.0), #C1C6D7 (footnote text, normal text >= 4.5)
    - All six assertions should PASS immediately (contrast values are confirmed in RESEARCH.md)
  </behavior>
  <action>
    Create `src/utils/contrast.test.ts` (new file — the utils/ directory may not exist yet; create it).

    Implement a local `getContrastRatio(hex1: string, hex2: string): number` helper inline in the test file using the WCAG relative luminance formula:
    1. Parse hex to RGB (0–1 range)
    2. Linearize each channel: c <= 0.04045 ? c/12.92 : ((c+0.055)/1.055)^2.4
    3. Luminance L = 0.2126*R + 0.7152*G + 0.0722*B
    4. Contrast = (Lhigher + 0.05) / (Llower + 0.05)

    Write one `it()` per color. The background is `#131313`. Assert `contrastRatio >= 3.0` for route colors and heatmap endpoints (UI component threshold), and `>= 4.5` for footnote text (#C1C6D7).

    These tests are intentionally "pre-passing" — they document the color contract and will catch any future color regressions. All six should be GREEN on first run.

    Run `npx vitest run src/utils/contrast.test.ts` to confirm all pass.
  </action>
  <verify>
    <automated>npx vitest run src/utils/contrast.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Six contrast assertions all pass green. The helper function is correct (Route A #A7C8FF should yield ~10.93:1 per RESEARCH.md). File runs without TypeScript errors.</done>
</task>

</tasks>

<verification>
Run full test suite after all three tasks complete:
`npx vitest run --reporter=verbose`

Expected result:
- contrast.test.ts: all 6 green
- MapCanvas.test.tsx: new DATA-03 and DATA-05 tests are RED (implementations not built yet)
- Sidebar.test.tsx: DATA-04 error branch tests are RED (implementation not built yet)

This is the correct Wave 0 state — scaffolds exist, implementations are pending.
</verification>

<success_criteria>
- Three test files exist on disk
- `npx vitest run` runs without crashing (no import errors, no missing modules)
- contrast.test.ts: 6/6 green
- MapCanvas.test.tsx: at least 3 new tests visible in output (failing is expected)
- Sidebar.test.tsx: at least 2 new tests visible in output (failing is expected)
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-hardening-polish/05-01-SUMMARY.md`
</output>
