---
phase: 04-pdf-dossier-export
plan: 03
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - server/src/pdf/pdfGenerator.ts
  - server/src/pdf/template.ejs
autonomous: true
requirements:
  - PDF-02
  - PDF-03

must_haves:
  truths:
    - "generatePdf(data) returns a Buffer (Puppeteer PDF output, no disk I/O)"
    - "Puppeteer browser launches once as a module-level singleton — not per request"
    - "template.ejs renders all 6 pages with CSS page-break-after sections"
    - "EJS template uses only ejs.render() on a string — no Express view engine required"
    - "Template has defensive defaults — no section renders the string 'undefined'"
    - "cd server && npx vitest run passes (pdfGenerator tests pass where automatable)"
  artifacts:
    - path: "server/src/pdf/pdfGenerator.ts"
      provides: "Puppeteer singleton + generatePdf(PdfTemplateData): Promise<Buffer>"
      exports: ["generatePdf", "PdfTemplateData", "getBrowser"]
    - path: "server/src/pdf/template.ejs"
      provides: "Full 6-page HTML/EJS dossier template"
  key_links:
    - from: "server/src/pdf/pdfGenerator.ts"
      to: "server/src/pdf/template.ejs"
      via: "fs.readFile(__dirname/template.ejs) + ejs.render()"
    - from: "server/src/pdf/pdfGenerator.ts"
      to: "puppeteer"
      via: "getBrowser() singleton + page.setContent() + page.pdf()"
---

<objective>
Build the Puppeteer PDF rendering engine: the singleton browser manager, the generatePdf() function, and the full EJS HTML template for all 6 dossier pages.

Purpose: This is the core PDF rendering layer. It runs entirely server-side using Puppeteer headless Chrome — no client-side canvas capture, no disk writes. The template implements the exact 6-page story arc from CONTEXT.md.
Output: pdfGenerator.ts (Puppeteer singleton + generatePdf) and template.ejs (full 6-page white PDF template). Parallel with 04-02 — no shared files.
</objective>

<execution_context>
@C:/Users/hudan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hudan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pdf-dossier-export/04-CONTEXT.md
@.planning/phases/04-pdf-dossier-export/04-RESEARCH.md

<interfaces>
<!-- PdfTemplateData shape — the single data object passed to generatePdf() and ejs.render() -->
```typescript
interface PdfTemplateData {
  route: RouteResult;                      // from src/types.ts
  recommendation: RouteRecommendation;     // from src/types.ts
  triggers: EnvironmentalTrigger[];        // from src/types.ts (per-route array)
  alerts: SierraAlert;                     // from src/types.ts
  projectSummary: ProjectSummary;          // from src/types.ts
  narrative: string;                       // 3-paragraph HTML string
  contacts: MockContact[];                 // from server/src/data/mock-contacts.ts
  mapThumbnail: string;                    // base64 data URI or '' if unavailable
  exportDate: string;                      // formatted date string, e.g. "April 16, 2026"
}
```

<!-- 6-page structure from CONTEXT.md -->
// Page 1: Cover — Sierra branding, route label, export date, map thumbnail, narrative intro
// Page 2: Route metrics + Sierra Recommends rationale
// Page 3: Environmental Trigger summary + Sierra Alerts risk flag
// Page 4: Inline Project Summary phase timeline
// Page 5: Per-segment justifications
// Page 6: Mock land parcel owner contacts table

<!-- Route color scheme (print-safe, darkened from UI colors for legibility on white) -->
// Route A: #5B9BD5 (darkened from #A7C8FF)
// Route B: #D48A2A (darkened from #FFBC7C)
// Route C: #9B6BBF (darkened from #E8B3FF)

<!-- Puppeteer anti-patterns to avoid (from research) -->
// - Do NOT set `path` in page.pdf() — receive Buffer directly
// - Do NOT launch browser per request — use module singleton
// - Do NOT use waitUntil: 'domcontentloaded' — use 'networkidle0'
// - Do NOT reference Google Fonts via CDN — embed fonts as base64 or use system fonts
// - headless: true (boolean) — NOT headless: 'new' (deprecated since v22)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Puppeteer singleton and generatePdf function</name>
  <files>server/src/pdf/pdfGenerator.ts</files>
  <behavior>
    - Module exports: generatePdf(data: PdfTemplateData): Promise<Buffer>, PdfTemplateData interface, getBrowser() for test access
    - getBrowser(): singleton pattern — launches once, returns same instance on subsequent calls
    - Puppeteer launch args: headless: true (not 'new'), --no-sandbox, --disable-setuid-sandbox, --disable-dev-shm-usage, --disable-gpu
    - generatePdf(): reads template.ejs from disk, calls ejs.render(templateStr, data), calls page.setContent(html, { waitUntil: 'networkidle0' }), calls page.pdf() WITHOUT path option (returns Buffer)
    - page.pdf() options: format A4, printBackground true, displayHeaderFooter true, margin top 60px bottom 50px left 40px right 40px
    - Footer template: "Sierra — Illustrative data only" left + "Page N of M" right, font-size 9px system font only (no Google Fonts in footer)
    - page.close() in finally block — browser singleton never closed mid-execution
    - Defensive defaults applied to templateData before ejs.render(): coalesce undefined narrative to empty string, undefined contacts to [], undefined triggers to []
  </behavior>
  <action>
Create `server/src/pdf/pdfGenerator.ts`:

```typescript
import puppeteer from 'puppeteer';
import type { Browser } from 'puppeteer';
import ejs from 'ejs';
import fs from 'fs/promises';
import path from 'path';
import { fileURLToPath } from 'url';
import type { RouteResult, RouteRecommendation, EnvironmentalTrigger, SierraAlert, ProjectSummary } from '../../../src/types.js';
import type { MockContact } from '../data/mock-contacts.js';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// ─── Type Definitions ─────────────────────────────────────────────────────
export interface PdfTemplateData {
  route: RouteResult;
  recommendation: RouteRecommendation;
  triggers: EnvironmentalTrigger[];
  alerts: SierraAlert;
  projectSummary: ProjectSummary;
  narrative: string;
  contacts: MockContact[];
  mapThumbnail: string;
  exportDate: string;
}

// ─── Puppeteer Singleton ──────────────────────────────────────────────────
// Anti-pattern avoided: Do not launch per request — Chrome startup is 2-4s.
// Launch once at first PDF request; reuse for all subsequent requests.
let _browser: Browser | null = null;

export async function getBrowser(): Promise<Browser> {
  if (!_browser) {
    _browser = await puppeteer.launch({
      headless: true, // headless: 'new' is deprecated since v22; boolean is correct
      args: [
        '--no-sandbox',
        '--disable-setuid-sandbox',
        '--disable-dev-shm-usage',
        '--disable-gpu',
      ],
    });
  }
  return _browser;
}

// Register cleanup so orphaned Chrome processes don't accumulate on restart
process.on('exit', () => { _browser?.close(); });
process.on('SIGINT', () => { _browser?.close(); process.exit(); });
process.on('SIGTERM', () => { _browser?.close(); process.exit(); });

// ─── PDF Generator ────────────────────────────────────────────────────────
export async function generatePdf(data: PdfTemplateData): Promise<Buffer> {
  // Defensive defaults — never render 'undefined' in the template
  const safeData: PdfTemplateData = {
    ...data,
    narrative: data.narrative || '',
    contacts: data.contacts || [],
    triggers: data.triggers || [],
    mapThumbnail: data.mapThumbnail || '',
    exportDate: data.exportDate || new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }),
  };

  const templatePath = path.join(__dirname, 'template.ejs');
  const templateStr = await fs.readFile(templatePath, 'utf-8');
  const html = ejs.render(templateStr, safeData, { filename: templatePath });

  const browser = await getBrowser();
  const page = await browser.newPage();

  try {
    // waitUntil: 'networkidle0' — safe for base64 data URIs and avoids external font timeouts
    await page.setContent(html, { waitUntil: 'networkidle0' });

    const pdfBuffer = await page.pdf({
      format: 'A4',
      printBackground: true,
      displayHeaderFooter: true,
      headerTemplate: '<span></span>',
      footerTemplate: `
        <div style="font-size:9px;font-family:Arial,sans-serif;color:#888;
                    width:100%;padding:0 40px;box-sizing:border-box;
                    display:flex;justify-content:space-between;align-items:center;">
          <span>Sierra &mdash; Illustrative data only</span>
          <span>Page <span class="pageNumber"></span> of <span class="totalPages"></span></span>
        </div>`,
      // margin.bottom must accommodate footer height (50px minimum for 9px text)
      margin: { top: '60px', bottom: '50px', left: '40px', right: '40px' },
    });

    // Puppeteer v24 returns Uint8Array — convert to Node Buffer for Express res.send()
    return Buffer.from(pdfBuffer);
  } finally {
    await page.close(); // always close Page, never Browser
  }
}
```

Now fill in the pdfGenerator test stubs to be as GREEN as possible. Note: Full Puppeteer execution requires a real Chrome binary and cannot run in jsdom — those tests remain todo. The data contract tests can be filled:

Update `server/src/__tests__/pdfGenerator.test.ts`:

```typescript
import { describe, test, expect, vi } from 'vitest';
import type { PdfTemplateData } from '../pdf/pdfGenerator.js';

// Note: Full Puppeteer execution (generatePdf returns Buffer) requires headless Chrome
// and cannot run in the jsdom Vitest environment. Those tests remain .todo.
// Data contract and defensive defaults are tested via type checking and unit stubs.

describe('pdfGenerator', () => {
  test.todo('generatePdf returns a Buffer');

  test('PdfTemplateData interface includes all required fields', () => {
    // Type-level contract test: construct a minimal PdfTemplateData to verify shape
    const minimalData: Partial<PdfTemplateData> = {
      narrative: 'test narrative',
      contacts: [],
      triggers: [],
      mapThumbnail: '',
      exportDate: 'April 16, 2026',
    };
    // If the interface changes and required fields are removed, this test catches it
    expect(minimalData.narrative).toBeDefined();
    expect(minimalData.contacts).toBeDefined();
    expect(minimalData.exportDate).toBeDefined();
  });

  test.todo('generatePdf template renders without "undefined" strings when all fields provided');
  test.todo('Puppeteer browser singleton is reused across calls — not launched per request');
});
```
  </action>
  <verify>
    <automated>cd server && npx vitest run src/__tests__/pdfGenerator.test.ts --reporter=verbose</automated>
  </verify>
  <done>pdfGenerator.ts compiles without TypeScript errors; exports generatePdf, PdfTemplateData, getBrowser; Puppeteer singleton registered with process cleanup handlers; test file passes (todo stubs + 1 passing contract test)</done>
</task>

<task type="auto">
  <name>Task 2: Build EJS dossier template (6 pages)</name>
  <files>server/src/pdf/template.ejs</files>
  <action>
Create `server/src/pdf/template.ejs` — the complete HTML/CSS/EJS template for the PDF dossier.

Requirements from CONTEXT.md:
- White background, dark text (#1a1a1a), route colors used as accent only (print-safe darkened versions)
- Cover page: Sierra branding, route label with route color accent, export date, full-width map thumbnail, narrative paragraphs
- Running header NOT needed (Puppeteer footer handles page numbers) — cover has its own title block
- Pages 2-6: section title in uppercase with letter-spacing, content, CSS page breaks
- Table styling: thin borders (#eee), alternating header background (#f5f5f5), 11px font
- Fonts: Inter, system-ui, sans-serif (NO external CDN — avoids networkidle0 timeout)
- All mock data marked with "Illustrative data only" in footnote text (already in footer template)
- EJS syntax: <%= value %> for escaped output, <%- value %> for unescaped HTML (narrative paragraphs), <% %> for logic

Page structure (use div.page with page-break-after: always except last):
1. Cover (page 1): Sierra header + route label + export date + map thumbnail + narrative
2. Metrics + Recommendation (page 2): distance/cost/permitting table + recommendation rationale
3. Environmental Triggers + Alerts (page 3): trigger list per-route + alerts callout
4. Project Timeline (page 4): phase timeline table
5. Segment Justifications (page 5): table of segment index + justification text
6. Mock Contacts (page 6): parcel owner contact table

For triggers on page 3, show only the triggers for the recommended route (data.recommendation.routeId). Show other routes collapsed as a sub-table or simple list.

For segment justifications on page 5, iterate over data.route.segmentJustifications (a Record<number, string> or array — use Object.entries if Record).

The template must handle missing/empty data gracefully — use EJS conditionals: `<% if (mapThumbnail) { %>...<% } %>`, `<% if (contacts && contacts.length) { %>...<% } %>`.

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Sierra Transmission Routing Dossier</title>
<style>
  /* ── Reset & Base ─────────────────────────────────── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    font-family: Inter, system-ui, -apple-system, sans-serif;
    background: #ffffff;
    color: #1a1a1a;
    font-size: 12px;
    line-height: 1.55;
  }

  /* ── Page Breaks ──────────────────────────────────── */
  .page {
    padding: 32px 40px 40px;
    min-height: 100vh;
    page-break-after: always;
    break-after: page;
  }
  .page:last-child {
    page-break-after: auto;
    break-after: auto;
  }

  /* ── Typography ───────────────────────────────────── */
  h1 { font-size: 22px; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 4px; }
  h2 { font-size: 15px; font-weight: 600; margin-bottom: 20px; }
  .section-title {
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #666;
    margin-bottom: 14px;
    padding-bottom: 6px;
    border-bottom: 1px solid #e0e0e0;
  }
  p { margin-bottom: 12px; }

  /* ── Route Color Accents (print-safe, darkened) ───── */
  .accent-A { color: #5B9BD5; }
  .accent-B { color: #D48A2A; }
  .accent-C { color: #9B6BBF; }
  .badge {
    display: inline-block;
    padding: 3px 10px;
    border-radius: 4px;
    font-size: 11px;
    font-weight: 700;
    color: #fff;
  }
  .badge-A { background: #5B9BD5; }
  .badge-B { background: #D48A2A; }
  .badge-C { background: #9B6BBF; }

  /* ── Cover Page ───────────────────────────────────── */
  .cover-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 24px;
  }
  .sierra-wordmark {
    font-size: 18px;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #1a1a1a;
  }
  .export-meta {
    font-size: 10px;
    color: #999;
    text-align: right;
  }
  .cover-route-label {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 6px;
  }
  .cover-subtitle {
    font-size: 12px;
    color: #666;
    margin-bottom: 24px;
  }
  .cover-map {
    width: 100%;
    height: 280px;
    object-fit: cover;
    border-radius: 6px;
    margin-bottom: 28px;
    background: #f0f0f0;
  }
  .cover-map-placeholder {
    width: 100%;
    height: 280px;
    background: #f0f4f8;
    border: 1px solid #dce3ea;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #999;
    font-size: 11px;
    margin-bottom: 28px;
  }
  .narrative { font-size: 11.5px; line-height: 1.65; }

  /* ── Tables ───────────────────────────────────────── */
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 11px;
    margin-bottom: 20px;
  }
  thead th {
    background: #f5f5f5;
    padding: 8px 10px;
    text-align: left;
    font-weight: 700;
    border-bottom: 2px solid #e0e0e0;
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: #555;
  }
  tbody td {
    padding: 7px 10px;
    border-bottom: 1px solid #eeeeee;
    vertical-align: top;
  }
  tbody tr:last-child td { border-bottom: none; }

  /* ── Metrics Block ────────────────────────────────── */
  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-bottom: 24px;
  }
  .metric-card {
    background: #f9f9f9;
    border: 1px solid #e8e8e8;
    border-radius: 6px;
    padding: 14px 16px;
  }
  .metric-label {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #888;
    margin-bottom: 4px;
  }
  .metric-value {
    font-size: 18px;
    font-weight: 700;
    color: #1a1a1a;
  }
  .metric-unit {
    font-size: 11px;
    color: #666;
    margin-top: 2px;
  }

  /* ── Recommendation Box ───────────────────────────── */
  .recommendation-box {
    border-left: 4px solid #5B9BD5;
    padding: 14px 18px;
    background: #f6faff;
    border-radius: 0 6px 6px 0;
    margin-bottom: 20px;
  }
  .recommendation-box.accent-A { border-left-color: #5B9BD5; background: #f6faff; }
  .recommendation-box.accent-B { border-left-color: #D48A2A; background: #fdf8f0; }
  .recommendation-box.accent-C { border-left-color: #9B6BBF; background: #faf6fd; }
  .recommendation-label {
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #888;
    margin-bottom: 6px;
  }
  .recommendation-text { font-size: 11.5px; line-height: 1.6; }

  /* ── Alert Box ────────────────────────────────────── */
  .alert-box {
    border: 1px solid #f0c040;
    background: #fffbec;
    border-radius: 6px;
    padding: 14px 18px;
    margin-bottom: 12px;
  }
  .alert-label {
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #b8860b;
    margin-bottom: 6px;
  }
  .alert-text { font-size: 11px; line-height: 1.55; }
  .alert-location {
    display: inline-block;
    font-size: 10px;
    background: #f0c040;
    color: #5a4000;
    padding: 2px 7px;
    border-radius: 3px;
    margin-top: 6px;
    font-weight: 600;
  }

  /* ── Timeline Table ───────────────────────────────── */
  .timeline-total td {
    font-weight: 700;
    background: #f5f5f5;
    border-top: 2px solid #d0d0d0;
  }

  /* ── Illustrative Disclaimer ──────────────────────── */
  .footnote {
    font-size: 9px;
    color: #aaa;
    margin-top: 20px;
    padding-top: 10px;
    border-top: 1px solid #eee;
  }
</style>
</head>
<body>

<!-- ═══════════════════════════════════════════════════════
     PAGE 1: COVER
     ═══════════════════════════════════════════════════════ -->
<div class="page">
  <div class="cover-header">
    <div class="sierra-wordmark">Sierra</div>
    <div class="export-meta">
      Transmission Routing Analysis<br>
      Export Date: <%= exportDate %>
    </div>
  </div>

  <div class="cover-route-label accent-<%= route.id %>">
    <% if (route.id === 'A') { %>Route A &mdash; Lowest Cost<% } %>
    <% if (route.id === 'B') { %>Route B &mdash; Balanced<% } %>
    <% if (route.id === 'C') { %>Route C &mdash; Lowest Regulatory Risk<% } %>
  </div>
  <div class="cover-subtitle">Sierra Transmission Routing Dossier &mdash; ERCOT Texas Corridor</div>

  <% if (mapThumbnail) { %>
    <img class="cover-map" src="<%= mapThumbnail %>" alt="Route map thumbnail showing corridor across Texas" />
  <% } else { %>
    <div class="cover-map-placeholder">Map thumbnail unavailable &mdash; Mapbox Static API not reachable</div>
  <% } %>

  <div class="narrative">
    <%- narrative %>
  </div>

  <div class="footnote">All data shown in this dossier is illustrative and generated for demonstration purposes only. No reliance should be placed on this analysis for actual engineering, legal, or financial decisions.</div>
</div>


<!-- ═══════════════════════════════════════════════════════
     PAGE 2: ROUTE METRICS + SIERRA RECOMMENDS
     ═══════════════════════════════════════════════════════ -->
<div class="page">
  <div class="section-title">Route Profile &amp; Key Metrics</div>

  <div class="metrics-grid">
    <div class="metric-card">
      <div class="metric-label">Distance</div>
      <div class="metric-value accent-<%= route.id %>"><%= route.metrics ? route.metrics.distanceMiles : '—' %></div>
      <div class="metric-unit">miles</div>
    </div>
    <div class="metric-card">
      <div class="metric-label">Estimated Cost</div>
      <div class="metric-value"><%= route.metrics ? route.metrics.estimatedCostM : '—' %></div>
      <div class="metric-unit">$ million (illustrative)</div>
    </div>
    <div class="metric-card">
      <div class="metric-label">Permitting Timeline</div>
      <div class="metric-value"><%= route.metrics ? route.metrics.permittingMonths : '—' %></div>
      <div class="metric-unit">months (illustrative)</div>
    </div>
  </div>

  <div class="section-title">Sierra Recommends</div>
  <% if (recommendation) { %>
  <div class="recommendation-box accent-<%= recommendation.routeId %>">
    <div class="recommendation-label">Sierra Recommends: Route <%= recommendation.routeId %></div>
    <div class="recommendation-text"><%= recommendation.rationale %></div>
  </div>
  <% } %>

  <div class="footnote">All metrics are illustrative estimates. Actual costs and timelines depend on detailed engineering, regulatory, and market conditions.</div>
</div>


<!-- ═══════════════════════════════════════════════════════
     PAGE 3: ENVIRONMENTAL TRIGGERS + SIERRA ALERTS
     ═══════════════════════════════════════════════════════ -->
<div class="page">
  <div class="section-title">Environmental &amp; Regulatory Triggers</div>

  <% var routeTriggers = triggers.find(function(t) { return t.routeId === route.id; }); %>
  <% if (routeTriggers && routeTriggers.triggers && routeTriggers.triggers.length) { %>
  <table>
    <thead>
      <tr>
        <th style="width:22%">Statute</th>
        <th style="width:50%">Plain-English Summary</th>
        <th style="width:28%">Timeline Impact</th>
      </tr>
    </thead>
    <tbody>
      <% routeTriggers.triggers.forEach(function(trigger) { %>
      <tr>
        <td><strong><%= trigger.statute %></strong></td>
        <td><%= trigger.explanation %></td>
        <td>
          <% if (trigger.timelineMonths && trigger.timelineMonths[1] > 0) { %>
            Approx. <%= trigger.timelineMonths[0] %>&ndash;<%= trigger.timelineMonths[1] %> months
          <% } else { %>
            No additional timeline impact
          <% } %>
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  <% } %>

  <div class="section-title" style="margin-top:24px;">Sierra Alerts &mdash; Critical Risk</div>

  <% if (alerts && alerts.primary) { %>
  <div class="alert-box">
    <div class="alert-label">&#9888; Critical Risk Identified</div>
    <div class="alert-text"><%= alerts.primary.text %></div>
    <% if (alerts.primary.location) { %>
    <div class="alert-location"><%= alerts.primary.location %></div>
    <% } %>
  </div>

  <% if (alerts.secondary && alerts.secondary.length) { %>
    <% alerts.secondary.forEach(function(alert) { %>
    <div class="alert-box" style="background:#fffdf5;border-color:#e0d090;">
      <div class="alert-label" style="color:#9a7a00;">&#9888; Secondary Risk</div>
      <div class="alert-text"><%= alert.text %></div>
      <% if (alert.location) { %><div class="alert-location" style="background:#e0d090;color:#4a3a00;"><%= alert.location %></div><% } %>
    </div>
    <% }); %>
  <% } %>
  <% } %>

  <div class="footnote">Regulatory analysis is illustrative. Actual permit requirements depend on final route alignment and agency consultation outcomes.</div>
</div>


<!-- ═══════════════════════════════════════════════════════
     PAGE 4: INLINE PROJECT SUMMARY (PHASE TIMELINE)
     ═══════════════════════════════════════════════════════ -->
<div class="page">
  <div class="section-title">Inline Project Summary &mdash; Phase Timeline</div>

  <% if (projectSummary && projectSummary.phases && projectSummary.phases.length) { %>
  <table>
    <thead>
      <tr>
        <th style="width:35%">Phase</th>
        <th style="width:20%">Estimated Duration</th>
        <th style="width:45%">Key Dependency</th>
      </tr>
    </thead>
    <tbody>
      <% projectSummary.phases.forEach(function(phase, idx) { %>
      <tr class="<%= idx === projectSummary.phases.length - 1 ? 'timeline-total' : '' %>">
        <td><%= phase.name %></td>
        <td>
          <% if (phase.estimatedMonths && phase.estimatedMonths[1] > 0) { %>
            <%= phase.estimatedMonths[0] %>&ndash;<%= phase.estimatedMonths[1] %> months
          <% } else { %>
            See phases above
          <% } %>
        </td>
        <td style="color:#555;font-size:10.5px;"><%= phase.keyDependency %></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  <% } %>

  <div class="footnote">Timeline estimates are illustrative only. Parallel-tracking permitting and ROW acquisition is critical to schedule compression. Actual timelines depend on agency workload and landowner cooperation.</div>
</div>


<!-- ═══════════════════════════════════════════════════════
     PAGE 5: PER-SEGMENT JUSTIFICATIONS
     ═══════════════════════════════════════════════════════ -->
<div class="page">
  <div class="section-title">Per-Segment Friction Justifications</div>

  <% var justifications = route.segmentJustifications; %>
  <% if (justifications && Object.keys(justifications).length) { %>
  <table>
    <thead>
      <tr>
        <th style="width:12%">Segment</th>
        <th>AI-Generated Friction Justification</th>
      </tr>
    </thead>
    <tbody>
      <% Object.entries(justifications).forEach(function(entry) { %>
      <tr>
        <td style="font-weight:600;color:#666;">#<%= entry[0] %></td>
        <td><%= entry[1] %></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  <% } else { %>
  <p style="color:#888;font-size:11px;">Segment justification data not available for this route.</p>
  <% } %>

  <div class="footnote">Friction justifications are LLM-generated based on pre-computed constraint layer analysis. They are illustrative summaries, not engineering assessments.</div>
</div>


<!-- ═══════════════════════════════════════════════════════
     PAGE 6: MOCK LAND PARCEL CONTACTS
     ═══════════════════════════════════════════════════════ -->
<div class="page">
  <div class="section-title">Mock Land Parcel Owner Contacts</div>

  <% if (contacts && contacts.length) { %>
  <table>
    <thead>
      <tr>
        <th style="width:28%">Owner / Entity</th>
        <th style="width:14%">County</th>
        <th style="width:10%">Acreage</th>
        <th style="width:16%">Parcel Type</th>
        <th style="width:16%">Phone</th>
        <th style="width:16%">Email</th>
      </tr>
    </thead>
    <tbody>
      <% contacts.forEach(function(contact) { %>
      <tr>
        <td><%= contact.ownerName %></td>
        <td><%= contact.county %></td>
        <td style="text-align:right;"><%= contact.acreage.toLocaleString() %></td>
        <td style="font-size:10px;color:#555;"><%= contact.parcelType %></td>
        <td style="font-size:10px;"><%= contact.phone %></td>
        <td style="font-size:10px;color:#5B9BD5;"><%= contact.email %></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  <% } %>

  <div class="footnote">All contact information is fictional and provided for demonstration purposes only. No actual landowners or parcel data are represented. Real ROW acquisition requires professional title research and registered mail outreach per Texas Property Code Chapter 21.</div>
</div>

</body>
</html>
```
  </action>
  <verify>
    <automated>cd server && node -e "import('./src/pdf/pdfGenerator.js').then(m => { console.log('exports:', Object.keys(m).join(', ')); process.exit(0); }).catch(e => { console.error(e.message); process.exit(1); })"</automated>
  </verify>
  <done>pdfGenerator.ts compiles and exports generatePdf, PdfTemplateData, getBrowser; template.ejs exists with all 6 page divs and correct EJS syntax; Puppeteer singleton includes process cleanup handlers</done>
</task>

</tasks>

<verification>
- `cd server && npx vitest run src/__tests__/pdfGenerator.test.ts --reporter=verbose` passes
- `cd server && node -e "import('./src/pdf/pdfGenerator.js').then(...)` loads without error
- template.ejs has 6 .page divs
- page-break-after: always on all pages except last
- No external font CDN references in template (system fonts only)
</verification>

<success_criteria>
generatePdf() function is ready to receive a PdfTemplateData object and return a Buffer. EJS template covers all 6 pages. Both modules can be imported by the PDF endpoint in 04-05.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pdf-dossier-export/04-03-SUMMARY.md`
</output>
