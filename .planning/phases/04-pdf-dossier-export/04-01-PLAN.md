---
phase: 04-pdf-dossier-export
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/TopNav/TopNav.test.tsx
  - server/src/__tests__/pdfGenerator.test.ts
  - server/src/__tests__/buildMapboxUrl.test.ts
  - server/src/__tests__/narrative.test.ts
autonomous: true
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04

must_haves:
  truths:
    - "All four test scaffold files exist with test.todo stubs — no imports of missing modules"
    - "puppeteer, ejs, @mapbox/polyline, @types/ejs are installed in server/node_modules"
    - "npx vitest run (root) exits 0"
    - "cd server && npx vitest run exits 0"
  artifacts:
    - path: "src/components/TopNav/TopNav.test.tsx"
      provides: "PDF-01 test scaffold — button enabled/disabled by simulationStatus"
    - path: "server/src/__tests__/pdfGenerator.test.ts"
      provides: "PDF-03 test scaffold — generatePdf data contract + template render"
    - path: "server/src/__tests__/buildMapboxUrl.test.ts"
      provides: "PDF-04 test scaffold — URL structure, bbox, polyline encoding, coordinate swap"
    - path: "server/src/__tests__/narrative.test.ts"
      provides: "PDF-02 partial — canned narrative fallback shape validation"
  key_links:
    - from: "server/src/__tests__/buildMapboxUrl.test.ts"
      to: "server/src/pdf/buildMapboxUrl.ts"
      via: "import — stub references file Wave 1 will create"
    - from: "server/src/__tests__/pdfGenerator.test.ts"
      to: "server/src/pdf/pdfGenerator.ts"
      via: "import — stub references file Wave 1 will create"
---

<objective>
Install Phase 4 server dependencies and create all four test scaffold files in RED state. This is the Wave 0 gate — no production code is written here.

Purpose: Establish the Nyquist sampling contract. All automated verify commands in subsequent plans depend on these scaffolds existing before implementation begins.
Output: 4 test files + server dependency installs. Both test runners exit 0 (stubs use test.todo — no imports of non-existent modules).
</objective>

<execution_context>
@C:/Users/hudan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hudan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pdf-dossier-export/04-CONTEXT.md
@.planning/phases/04-pdf-dossier-export/04-RESEARCH.md
@.planning/phases/04-pdf-dossier-export/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install server dependencies</name>
  <files>server/package.json</files>
  <action>
Run from the server directory:

```
cd server && npm install puppeteer ejs @mapbox/polyline @types/ejs
```

If `server/package.json` does not exist (Phase 3 not yet executed), this will fail. In that case, verify the server directory exists before running. Do NOT create the server scaffold here — that belongs to Phase 2/3.

Do NOT modify any source files — this task is installs only. Verify all four packages land in `server/node_modules`.
  </action>
  <verify>
    <automated>node -e "require('./server/node_modules/puppeteer/package.json')" && node -e "require('./server/node_modules/ejs/package.json')" && echo "deps OK"</automated>
  </verify>
  <done>puppeteer, ejs, @mapbox/polyline, and @types/ejs present in server/node_modules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create server-side test scaffolds</name>
  <files>
    server/src/__tests__/pdfGenerator.test.ts,
    server/src/__tests__/buildMapboxUrl.test.ts,
    server/src/__tests__/narrative.test.ts
  </files>
  <behavior>
    - pdfGenerator.test.ts: test.todo stubs for: "generatePdf returns a Buffer", "generatePdf template renders without 'undefined' strings", "generatePdf accepts PdfTemplateData shape with all required fields", "Puppeteer browser singleton is reused across calls (not launched per request)"
    - buildMapboxUrl.test.ts: test.todo stubs for: "buildMapboxStaticUrl returns URL containing satellite-streets-v12", "bbox includes padding around route bounds", "polyline overlay is URL-encoded in path", "coordinate swap: lng/lat GeoJSON pairs become lat/lng for polyline encoding", "downsamples coordinates when route has more than 100 points", "fetchMapboxThumbnail returns base64 data URI string"
    - narrative.test.ts: test.todo stubs for: "CANNED_NARRATIVES has entries for route A, B, and C", "each canned narrative is a non-empty string with 3 paragraphs", "each canned narrative references at least one Texas location name"
    - All stubs use test.todo('description') — no imports of non-existent modules
    - All files import only describe and test from vitest
  </behavior>
  <action>
Create `server/src/__tests__/pdfGenerator.test.ts`:

```typescript
import { describe, test } from 'vitest';

describe('pdfGenerator', () => {
  test.todo('generatePdf returns a Buffer');
  test.todo('generatePdf template renders without "undefined" strings when all fields provided');
  test.todo('generatePdf accepts PdfTemplateData shape with all required fields');
  test.todo('Puppeteer browser singleton is reused across calls — not launched per request');
});
```

Create `server/src/__tests__/buildMapboxUrl.test.ts`:

```typescript
import { describe, test } from 'vitest';

describe('buildMapboxStaticUrl', () => {
  test.todo('returns URL string containing satellite-streets-v12 style');
  test.todo('bbox parameter includes 0.05-degree padding around route coordinate bounds');
  test.todo('polyline overlay is URL-encoded in the path segment');
  test.todo('coordinate swap: GeoJSON [lng, lat] pairs are reversed to [lat, lng] for polyline encoding');
  test.todo('downsamples route coordinates when LineString has more than 100 points');
});

describe('fetchMapboxThumbnail', () => {
  test.todo('returns a base64 data URI string starting with data:image/png;base64,');
  test.todo('throws an error when Mapbox API returns non-200 status');
});
```

Create `server/src/__tests__/narrative.test.ts`:

```typescript
import { describe, test } from 'vitest';

describe('canned narrative fallback', () => {
  test.todo('CANNED_NARRATIVES has entries for route A, B, and C');
  test.todo('each canned narrative is a non-empty string');
  test.todo('each canned narrative contains at least one Texas location name');
  test.todo('POST /api/narrative returns narrative string in response JSON');
  test.todo('POST /api/narrative falls back to canned narrative when Claude API unavailable');
});
```

Create the `server/src/__tests__/` directory if it does not exist already.
  </action>
  <verify>
    <automated>cd server && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 server test files exist; cd server && npx vitest run exits 0 with all tests as todo</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create client-side TopNav test scaffold</name>
  <files>src/components/TopNav/TopNav.test.tsx</files>
  <behavior>
    - TopNav.test.tsx stubs for: "Export PDF button is visible when simulationStatus is complete", "Export PDF button is disabled when simulationStatus is not complete", "Export PDF button calls exportPdf when clicked and status is complete", "Export PDF button has opacity 0.4 and cursor not-allowed when disabled"
    - Uses test.todo stubs only — no imports of TopNav component (avoids import chain issues)
    - Imports only describe and test from vitest
  </behavior>
  <action>
Create `src/components/TopNav/TopNav.test.tsx`:

```typescript
import { describe, test } from 'vitest';

describe('TopNav — Export PDF button', () => {
  test.todo('button is visible and enabled when simulationStatus is "complete"');
  test.todo('button is disabled (opacity 0.4, cursor not-allowed) when simulationStatus is not "complete"');
  test.todo('clicking the button when enabled calls the exportPdf function');
  test.todo('button does not trigger exportPdf when simulationStatus is "idle"');
  test.todo('button does not trigger exportPdf when simulationStatus is "streaming"');
});
```

Note: `src/components/TopNav/TopNav.test.tsx` may already exist from Phase 1. If it does, append these describe/test blocks to the existing file rather than overwriting it.
  </action>
  <verify>
    <automated>npx vitest run src/components/TopNav/TopNav.test.tsx --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>TopNav.test.tsx exists with PDF export stubs; npx vitest run exits 0</done>
</task>

</tasks>

<verification>
- `npx vitest run` at project root exits 0
- `cd server && npx vitest run` exits 0
- server/node_modules contains puppeteer, ejs, @mapbox/polyline, @types/ejs
- 4 test scaffold files exist with test.todo stubs only
</verification>

<success_criteria>
Wave 0 gate clear: both test runners pass, all four Phase 4 test scaffolds exist, server dependencies installed. Every subsequent plan's automated verify command is runnable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pdf-dossier-export/04-01-SUMMARY.md`
</output>
