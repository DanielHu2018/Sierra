---
phase: 04-pdf-dossier-export
plan: 07
type: execute
wave: 4
depends_on:
  - 04-06
files_modified: []
autonomous: false
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04

must_haves:
  truths:
    - "Clicking 'Export PDF Dossier' in TopNav downloads a .pdf file to the browser"
    - "Downloaded PDF opens in a PDF viewer without errors"
    - "PDF cover page shows: Sierra branding, route label with route color, export date, map thumbnail (or placeholder), narrative paragraphs"
    - "PDF pages 2-6 render all sections in correct order without 'undefined' text"
    - "PDF footer shows 'Sierra — Illustrative data only' and page numbers on all pages"
    - "Export PDF button is visibly disabled (opacity 0.4) before simulation runs"
    - "All mock data sections are labeled as illustrative"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete PDF dossier export feature. All automated code is complete at this point — this checkpoint confirms the visual and functional result is judge-presentable.

Purpose: PDF generation requires visual human review — Puppeteer output, map thumbnail rendering, and multi-page layout cannot be fully verified by automated tests.
Output: Confirmed working PDF export or list of issues to fix.
</objective>

<execution_context>
@C:/Users/hudan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hudan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-pdf-dossier-export/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete PDF Dossier Export feature:
- POST /api/export/pdf Express endpoint with Puppeteer PDF generation
- EJS 6-page HTML template (cover + metrics + env triggers + timeline + segments + contacts)
- Mapbox Static Images API map thumbnail (satellite-streets-v12) embedded as base64
- POST /api/narrative endpoint (pre-generates 3-paragraph narrative at simulation time)
- useExportPdf hook + TopNav button wired with simulationStatus guard
  </what-built>
  <how-to-verify>
1. Start the dev environment: `npm run dev` (Vite frontend) + `cd server && npm run dev` (Express)

2. Verify button is disabled before simulation:
   - Load the app at http://localhost:5173
   - Confirm "EXPORT PDF" button is visible in TopNav but has reduced opacity (0.4) and cursor: not-allowed
   - DO NOT click it yet

3. Run a full simulation to completion:
   - Drop a Source pin (click somewhere in West Texas on the map)
   - Drop a Destination pin (click somewhere in Central Texas)
   - Click "Run Simulation" and wait for the Agent Reasoning Stream to complete (20-40 seconds)
   - Wait for all three routes to appear and results dashboard to load
   - Confirm "EXPORT PDF" button is now enabled (full opacity, pointer cursor)

4. Export the PDF (Route C recommended):
   - If Route C is not already selected, click it on the map or dashboard
   - Click "EXPORT PDF" button
   - Verify a file download dialog appears or a file named "sierra-dossier-route-C.pdf" downloads automatically
   - Open the downloaded PDF in a PDF viewer

5. Verify PDF visual quality — check each page:
   - Page 1 (Cover): Sierra wordmark top-left, export date top-right, route label with route color (purple for Route C), map thumbnail showing satellite Texas imagery OR placeholder div if Mapbox unavailable, 3-paragraph narrative text visible
   - Page 2 (Metrics + Recommendation): Three metric cards (distance, cost, permitting), "Sierra Recommends" box with colored left border and rationale text
   - Page 3 (Env Triggers + Alerts): Trigger table with 4 rows (ESA, CWA, NHPA, NEPA), Alert box with yellow border and location badge
   - Page 4 (Timeline): Six-row phase timeline table, last row in bold (Total), footnote text
   - Page 5 (Segments): Segment justification table — if empty shows "Segment justification data not available"
   - Page 6 (Contacts): 10-row contact table for Route C (Edwards Plateau contacts)

6. Check for quality issues:
   - No page contains the word "undefined" as visible text
   - Footer on all pages (except cover if it appears) shows "Sierra — Illustrative data only" and page numbers
   - White background throughout — no dark mode colors
   - Text is readable at normal zoom (not pixelated — this is a real PDF, not a screenshot)

7. Test export for a different route:
   - Click Route A on the map or dashboard
   - Click "EXPORT PDF" again
   - Confirm file downloads as "sierra-dossier-route-A.pdf"
   - Open and verify contacts page shows Reeves County / Pecos Basin contacts (Route A contacts), not Edwards Plateau contacts

8. Test edge case — export without Mapbox token (optional):
   - If MAPBOX_TOKEN is not set in server .env, the cover page should show the placeholder div instead of a satellite image — not a broken PDF
  </how-to-verify>
  <resume-signal>
Type "approved" if the PDF exports correctly and passes visual review.
Type "issues: [describe]" if problems found — describe which pages/sections have issues so fixes can be targeted.
  </resume-signal>
</task>

</tasks>

<verification>
All verification is manual per the checkpoint instructions above.

Automated smoke test (run before starting manual review):
```bash
npx vitest run && cd server && npx vitest run
```
Full test suite must be green before requesting human review.
</verification>

<success_criteria>
PDF dossier export is judge-presentable:
- Export PDF button visible and correctly enabled/disabled by simulation state
- Downloaded PDF opens without error and contains all 6 sections in correct order
- No "undefined" text in any section
- White background, print-safe design, footer on all pages
- Route-specific contacts correctly reflect the exported route's geographic corridor
- Map thumbnail renders (satellite imagery) or graceful placeholder if Mapbox unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/04-pdf-dossier-export/04-07-SUMMARY.md`
</output>
