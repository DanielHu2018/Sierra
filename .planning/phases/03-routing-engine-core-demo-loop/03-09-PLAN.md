---
phase: 03-routing-engine-core-demo-loop
plan: 09
type: execute
wave: 4
depends_on:
  - 03-07
  - 03-08
files_modified: []
autonomous: false
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-05
  - AI-04
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - HEAT-01
  - HEAT-02
  - HEAT-03
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - REC-01
  - REC-02
  - REC-03
  - ENV-01
  - ENV-02
  - ENV-03
  - ALERT-01
  - ALERT-02
  - ALERT-03
  - SUMM-01
  - SUMM-02
  - SUMM-03
  - HOVER-01
  - HOVER-02

must_haves:
  truths:
    - "Judge can drop two pins and click Run Simulation without error"
    - "Sidebar transitions from controls to typewriter stream then to results — all in one session"
    - "Three color-coded routes appear on the map within 60 seconds of clicking Run Simulation"
    - "Sierra Recommends callout is the first visible results element"
    - "Radar chart shows three overlapping shapes in route colors"
    - "Sierra Alerts appears unprompted with Texas location reference"
    - "Environmental Trigger Panel recommended route section is expanded"
    - "Project Summary timeline is accessible in the sidebar"
    - "Hovering a route segment shows instant popup with pre-loaded justification text"
    - "Friction heatmap toggles on/off green-to-red"
    - "Full vitest suite is green"
  artifacts:
    - path: "src/components/Sidebar/Sidebar.tsx"
      provides: "Fully wired state machine"
    - path: "src/components/MapCanvas/RouteLayer.tsx"
      provides: "Route layer with hover + click"
    - path: "server/src/routing/astar.ts"
      provides: "A* engine"
  key_links:
    - from: "Sidebar Run Simulation button"
      to: "POST /api/route + GET /api/stream/reasoning"
      via: "runSimulation callback — routes fetched, stream opened simultaneously"
    - from: "RouteLayer click handler"
      to: "RouteCards highlight"
      via: "setSelectedRoute → Zustand → both components read selectedRoute"
---

<objective>
Human verification of the complete Phase 3 end-to-end demo arc. This is a checkpoint plan — Claude runs the full test suite first, then pauses for the judge (user) to exercise the app manually against all 9 Phase 3 success criteria.

Purpose: The full 8-step demo arc must work end-to-end before Phase 3 is marked complete. Automated tests cannot verify visual rendering, SSE timing, or mouse hover interactions — this checkpoint covers what automation cannot.
Output: Confirmed working demo arc OR a list of issues to address before marking phase complete.
</objective>

<execution_context>
@C:/Users/hudan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hudan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-routing-engine-core-demo-loop/03-CONTEXT.md
@.planning/phases/03-routing-engine-core-demo-loop/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start dev servers</name>
  <files></files>
  <action>
Run the full test suite to confirm all automated tests pass:

```bash
npx vitest run --reporter=verbose
cd server && npx vitest run --reporter=verbose
```

Fix any failing tests before proceeding to the checkpoint. All todo stubs are acceptable — only unexpected failures need resolution.

Then start both dev servers to prepare for manual verification:

```bash
# Terminal 1 (project root):
npm run dev

# Terminal 2 (server directory):
cd server && npm run dev
```

Confirm both servers are running:
- Vite client: http://localhost:5173
- Express server: http://localhost:3001

Confirm the Vite proxy is configured to forward `/api/*` → `http://localhost:3001` (check `vite.config.ts` — this was configured in Phase 2's 02-01-PLAN.md).
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot && cd server && npx vitest run --reporter=dot</automated>
  </verify>
  <done>Full test suite green (todos acceptable); both dev servers running; app accessible at http://localhost:5173</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 demo arc:
- A* routing engine (POST /api/route) producing 3 color-coded routes
- Agent Reasoning Stream (20–40 second SSE typewriter narration)
- Sidebar state machine (controls → stream → results)
- Sierra Recommends callout (first results element)
- Radar comparison chart (3 routes, 4 axes)
- Route cards (compact, expandable, map-synchronized)
- Sierra Alerts (primary + collapsed secondary, Texas location)
- Environmental Trigger Panel (per-route accordion, recommended expanded)
- Inline Project Summary (6-row timeline, illustrative disclaimer)
- Hover justification popup (pre-loaded, instant)
- Friction heatmap toggle (green→red)
  </what-built>
  <how-to-verify>
Open http://localhost:5173 and walk through each step:

**Step 1 — Pin Drop**
- Click anywhere in Texas to drop the Source pin; verify label appears and map recenters
- Click a second location to drop the Destination pin; verify both pins visible

**Step 2 — Run Simulation**
- Click "Run Simulation" button (should be enabled with both pins set)
- Verify: sidebar immediately transitions to Agent Reasoning Panel (constraint controls disappear)

**Step 3 — Agent Reasoning Stream (20–40 seconds)**
- Verify: typewriter text appears character by character
- Verify: narration mentions Texas locations (Reeves County, Edwards Aquifer, Nolan County, US-385 corridor)
- Verify: [×] cancel button is visible top-right of stream panel
- Verify: stream concludes with "Sierra Recommends: Route C. Preparing justification and risk summary."
- Verify: brief pause, then sidebar transitions to results view

**Step 4 — Three Routes on Map**
- Verify: three color-coded lines appear on the map (blue=Route A, orange=Route B, purple=Route C)
- Verify: Route C (purple) is brighter/thicker (pre-selected as recommended)

**Step 5 — Sierra Recommends (first element)**
- Verify: top of results sidebar shows "★ Sierra Recommends" callout with Route C label and 3-sentence rationale

**Step 6 — Radar Chart**
- Verify: radar/spider chart below Sierra Recommends shows 3 overlapping shapes in route colors
- Verify: four axis labels visible: Cost, Permitting, Congestion Relief, Regulatory Risk

**Step 7 — Route Cards**
- Verify: three route cards below the radar chart, Route C pre-highlighted
- Click Route A card: verify Route A highlighted on map (orange becomes brighter) and card expands
- Click Route A line on map: verify Route A card becomes highlighted in sidebar
- Click Route C card: verify Route C highlighted again

**Step 8 — Sierra Alerts (unprompted)**
- Scroll down in results sidebar
- Verify: "⚠ Sierra Alerts" section visible without any user action
- Verify: primary alert text references a specific Texas location
- Click "Show more risks": verify secondary alerts expand

**Step 9 — Environmental Trigger Panel**
- Scroll further down
- Verify: "Environmental Triggers" section shows per-route accordion
- Verify: Route C section is expanded by default showing ESA/CWA/NHPA/NEPA triggers
- Click Route A to expand: verify Route A triggers show

**Step 10 — Project Summary**
- Scroll to bottom
- Verify: "Project Timeline" section shows 6-row table
- Verify: illustrative disclaimer text visible below table

**Step 11 — Hover Justification**
- Hover over any route segment on the map
- Verify: popup appears instantly (no network delay) showing justification text and friction score bar

**Step 12 — Friction Heatmap**
- In the controls (need to reset to idle first by refreshing, OR locate the Overlays section if accessible in results)
- Toggle the Friction Heatmap overlay
- Verify: green-to-red heatmap layer appears over the Texas terrain

**Step 13 — Cancel Button**
- Drop two new pins, click Run Simulation again
- Within 5 seconds of stream starting, click [×] cancel button
- Verify: sidebar returns to constraint controls view (idle state)
  </how-to-verify>
  <resume-signal>
Type "approved" if all 13 steps pass, or describe which steps failed and what was observed (e.g., "Step 3 failed — typewriter text not appearing").
  </resume-signal>
</task>

</tasks>

<verification>
All automated tests pass. Human verified all 9 Phase 3 success criteria from ROADMAP.md:
1. Agent Reasoning Stream narrates for 20–40 seconds
2. Three color-coded routes appear in under 60 seconds
3. Radar chart visible immediately on route generation
4. Sierra Recommends appears as first results element
5. Environmental triggers show per-route with recommended expanded
6. Sierra Alerts appears unprompted with Texas location
7. Project Summary accessible without PDF export
8. Hover shows instant justification popup
9. Friction heatmap toggleable green→red
</verification>

<success_criteria>
Human approves all 13 verification steps. Phase 3 is marked complete in STATE.md. The full 8-step demo arc works end-to-end from pin drop through project summary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing-engine-core-demo-loop/03-09-SUMMARY.md`

Update `.planning/STATE.md`:
- stopped_at: "Phase 3 complete"
- progress.completed_phases: 3
- progress.completed_plans: 15 (6 Phase 1 + 4 Phase 2 + 9 Phase 3... adjust to actual counts)
</output>
