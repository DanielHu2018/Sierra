---
phase: 01-foundation-map-canvas
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - public/data/ercot-grid.geojson
  - public/data/land-boundary.geojson
  - public/data/wildlife-habitat.geojson
  - public/data/topography.geojson
autonomous: true
requirements: [MAP-08, DATA-01]

must_haves:
  truths:
    - "Four GeoJSON files exist in public/data/ and are each under 2MB"
    - "Each file is valid GeoJSON with a FeatureCollection containing features covering the Texas bounding box"
    - "No file makes any HTTP request to an external API — all data is static"
  artifacts:
    - path: "public/data/ercot-grid.geojson"
      provides: "Mock ERCOT transmission line features (LineString)"
      contains: "FeatureCollection"
    - path: "public/data/land-boundary.geojson"
      provides: "Mock Texas land boundary features (Polygon)"
      contains: "FeatureCollection"
    - path: "public/data/wildlife-habitat.geojson"
      provides: "Mock wildlife habitat zone features (Polygon)"
      contains: "FeatureCollection"
    - path: "public/data/topography.geojson"
      provides: "Mock topographic contour line features (LineString)"
      contains: "FeatureCollection"
  key_links:
    - from: "src/components/MapCanvas/OverlayLayers.tsx"
      to: "public/data/ercot-grid.geojson"
      via: "fetch('/data/ercot-grid.geojson') or import"
      pattern: "ercot-grid.geojson"
---

<objective>
Create stub GeoJSON data files for all four map overlay layers. These are mock datasets — minimal but structurally valid — that unblock the Map Canvas plan (Plan 04) which needs files to exist in public/data/ before overlay layers can be wired.

Purpose: MAP-08 requires pre-simplified GeoJSON at Texas scale. DATA-01 requires all geospatial layers to use static data (no live API calls). These mock files satisfy both constraints during Phase 1.
Output: Four valid GeoJSON FeatureCollections in public/data/, each under 2MB, covering Texas geography with representative geometry.
</objective>

<execution_context>
@C:/Users/hudan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hudan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-map-canvas/01-CONTEXT.md
@.planning/phases/01-foundation-map-canvas/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four mock GeoJSON overlay files</name>
  <files>
    public/data/ercot-grid.geojson,
    public/data/land-boundary.geojson,
    public/data/wildlife-habitat.geojson,
    public/data/topography.geojson
  </files>
  <action>
    Create the `public/data/` directory and write four stub GeoJSON files. These are representative mock datasets — not real data — sized to be visually meaningful on a Texas-scale map (zoom 6) without requiring any external data source or mapshaper processing (they are already minimal).

    Texas bounding box approximate: lng [-106.65, -93.51], lat [25.84, 36.50]
    Texas geographic center: [-99.9018, 31.9686]

    **`public/data/ercot-grid.geojson`** — LineString features simulating major transmission corridors:
    Create a FeatureCollection with 8–12 LineString features. Draw diagonal and cross lines across Texas representing transmission corridors between major cities (Dallas, Houston, San Antonio, Austin, Amarillo, El Paso). Each feature has properties: `{ "name": "corridor label", "voltage": "345kV" }`. This file should be under 100KB.

    Example feature shape:
    ```json
    {
      "type": "Feature",
      "properties": { "name": "Dallas–Houston 345kV", "voltage": "345kV" },
      "geometry": {
        "type": "LineString",
        "coordinates": [[-96.797, 32.777], [-95.369, 29.760]]
      }
    }
    ```

    Include corridors for these approximate routes (straight lines are fine for mock data):
    - Dallas [-96.797, 32.777] to Houston [-95.369, 29.760]
    - Houston [-95.369, 29.760] to San Antonio [-98.493, 29.424]
    - San Antonio [-98.493, 29.424] to Austin [-97.743, 30.267]
    - Austin [-97.743, 30.267] to Dallas [-96.797, 32.777]
    - Amarillo [-101.831, 35.222] to Dallas [-96.797, 32.777]
    - El Paso [-106.485, 31.762] to San Antonio [-98.493, 29.424]
    - Austin [-97.743, 30.267] to Midland [-102.077, 31.997]
    - Midland [-102.077, 31.997] to Amarillo [-101.831, 35.222]

    **`public/data/land-boundary.geojson`** — Polygon features simulating state/private land boundaries:
    Create a FeatureCollection with 6–8 Polygon features representing different land ownership zones across Texas. Simple rectangular polygons are fine. Properties: `{ "type": "private" | "state" | "federal", "name": "zone name" }`. Each polygon should cover roughly a 2° × 2° area at different locations across Texas. Under 100KB.

    Example zones:
    - West Texas Federal Land (around Big Bend area): bbox [-104, 29, -101, 31]
    - East Texas State Forest: bbox [-95, 31, -93.5, 33]
    - Central Texas Private: bbox [-100, 30, -97, 32]
    - Panhandle State Land: bbox [-103, 34, -100, 36.5]
    - Gulf Coast Mixed: bbox [-97, 26, -94, 28]
    - Hill Country Private: bbox [-100, 29.5, -97.5, 31.5]

    **`public/data/wildlife-habitat.geojson`** — Polygon features simulating habitat and ESA zones:
    Create a FeatureCollection with 5–7 Polygon features representing wildlife habitat and environmentally sensitive zones. Properties: `{ "type": "critical_habitat" | "esa_zone" | "wildlife_corridor", "species": "species name", "sensitivity": "high" | "medium" | "low" }`. Simple polygons covering representative habitat areas. Under 100KB.

    Example zones:
    - Golden-cheeked Warbler habitat (Hill Country): bbox [-99.5, 29.5, -97, 31]
    - Black-capped Vireo zone (central TX): bbox [-100, 29, -98, 31]
    - Whooping Crane corridor (coastal): bbox [-97.5, 27, -96, 29]
    - Ocelot habitat (south TX): bbox [-99.5, 26, -97, 27.5]
    - Big Thicket corridor (east TX): bbox [-94.5, 30, -93.5, 31.5]

    **`public/data/topography.geojson`** — LineString features simulating elevation contour lines:
    Create a FeatureCollection with 10–15 LineString features representing elevation contours across Texas. Properties: `{ "elevation": number, "unit": "ft" }`. Use horizontal lines at varying latitudes to simulate contour bands. Under 100KB.

    Example contours at elevations 500ft, 1000ft, 1500ft, 2000ft, 2500ft, 3000ft spanning Texas longitude range:
    - 500ft: lat ~31.5, from lng -106 to -93.5
    - 1000ft: lat ~31.0, shorter spans in west TX
    - 1500ft: lat ~30.5, primarily west TX
    - 2000ft: lat ~30.0, far west TX
    - 2500ft: lat ~29.5, mountain region
    - 3000ft: lat ~29.0, Davis Mountains area
    (Vary the coordinates to create natural-looking non-straight lines — add a few intermediate points)

    After writing all four files, verify file sizes are each under 2MB:
    ```bash
    ls -la public/data/
    ```
  </action>
  <verify>
    <automated>cd /c/Users/hudan/Developer/Sierra && node -e "
      const fs = require('fs');
      const files = ['ercot-grid','land-boundary','wildlife-habitat','topography'];
      let allValid = true;
      files.forEach(f => {
        const path = \`public/data/\${f}.geojson\`;
        const stat = fs.statSync(path);
        const data = JSON.parse(fs.readFileSync(path, 'utf8'));
        const valid = data.type === 'FeatureCollection' && Array.isArray(data.features) && data.features.length > 0 && stat.size < 2097152;
        console.log(\`\${f}: \${data.features.length} features, \${Math.round(stat.size/1024)}KB, valid=\${valid}\`);
        if (!valid) allValid = false;
      });
      process.exit(allValid ? 0 : 1);
    " 2>&1</automated>
  </verify>
  <done>All four GeoJSON files exist in public/data/. Each is a valid FeatureCollection with at least 5 features. Each file is under 2MB. Node validation script exits 0.</done>
</task>

</tasks>

<verification>
```bash
node -e "const fs = require('fs'); ['ercot-grid','land-boundary','wildlife-habitat','topography'].forEach(f => { const d = JSON.parse(fs.readFileSync(\`public/data/\${f}.geojson\`,'utf8')); console.log(f, d.type, d.features.length, 'features'); })"
```
All four files print FeatureCollection with feature count > 0.
</verification>

<success_criteria>
- public/data/ directory exists with exactly 4 .geojson files
- Each file is valid JSON with type="FeatureCollection" and features array with at least 5 entries
- Each file is under 2MB (MAP-08 compliance)
- No file contains any URL, API endpoint, or external reference (DATA-01 compliance)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-map-canvas/01-03-SUMMARY.md` following the summary template.
</output>
