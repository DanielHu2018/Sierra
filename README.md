# Sierra Planning

Product planning, requirements, and Aegis automation definitions for Sierra.

- `.planning/` — project state, roadmap, requirements, and research notes
- `agents/` — Aegis agent definitions (YAML)
- `flows/` — Aegis orchestration flows (YAML)

Copy `.env.example` to `.env` locally when integrating tooling that needs secrets; do not commit `.env`.
